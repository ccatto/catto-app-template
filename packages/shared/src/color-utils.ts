// Re-export from @catto/shared for backward compatibility
export {
  hexToRgb,
  rgbToHex,
  adjustBrightness,
  getLuminance,
  getContrastColor,
  deriveThemeColors,
  isValidHexColor,
} from '@catto/shared';
export type { RGB, DerivedThemeColors } from '@catto/shared';
